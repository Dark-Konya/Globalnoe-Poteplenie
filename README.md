# Globalnoe-Poteplenie
#Konstantin
#Timofei
#Zhazira

import telebot
import random
import os
import requests
print(os.listdir("images"))
API_TOKEN = '8396512413:AAHLroL2XEe1rrlisNaoYLHGRR1QbKvosfg'
bot = telebot.TeleBot(API_TOKEN)

# Handle '/start' and '/help'
@bot.message_handler(commands=['start'])
def send_welcome(message):
    bot.reply_to(message, """ вот команды для использования:/Start,/Facts,/Daily_tip,/homeactions,/travelchoices,/travelchoices,/ask,/reduceplan,
""")
# Handle all other messages with content_type 'text' (content_types defaults to ['text'])

@bot.message_handler(commands=["Facts"])
def send_welcome(message):
    factt = ["Океаны — главные «губки»: Океаны поглощают более 90% избыточного тепла, удерживаемого парниковыми газами. Без этой помощи атмосфера Земли прогрелась бы уже не на 1,5, а на десятки градусов.
", "Арктика греется быстрее всех: Температура в Арктике растет в 4 раза быстрее, чем в среднем по остальному миру. Это явление называют «арктическим усилением».", "Таяние льда меняет время: Массовое таяние полярных льдов перераспределяет массу воды ближе к экватору. Это немного замедляет вращение Земли, что в долгосрочной перспективе влияет на точное измерение времени.
", "Зомби-пожары: В Сибири и на Аляске участились «пожары-зомби». Они тлеют глубоко в торфяниках под снегом всю зиму, а весной, когда снег тает, вспыхивают с новой силой.", "CO2 на пике за миллионы лет: Концентрация углекислого газа в атмосфере сейчас выше, чем когда-либо за последние 3 миллиона лет. Тогда уровень моря был на 10–20 метров выше, а в Арктике росли деревья.", "Влажность растет: Теплый воздух удерживает больше влаги (примерно на 7% на каждый градус потепления). Это приводит к тому, что дожди становятся гораздо мощнее, вызывая катастрофические наводнения.
", "Угроза облакам: Ученые опасаются, что при экстремальном росте температур слоисто-кучевые облака (которые отражают солнечный свет обратно в космос) могут просто исчезнуть, что резко ускорит нагрев планеты.", "«Климатические беженцы» среди животных: Многие виды рыб и птиц уже мигрируют ближе к полюсам. Например, атлантическая треска из-за потепления воды массово уходит в воды Арктики.
Смена цвета планеты: Из космоса Земля становится менее «белой» и более «темной» из-за таяния льдов. Темная поверхность поглощает еще больше солнечного тепла вместо того, чтобы отражать его.
", "Экономика под ударом: По прогнозам, если не остановить потепление, к 2050 году мировая экономика может терять до 10% своего ВВП ежегодно из-за природных катастроф и неурожаев.", ]
    fact = random.choice(factt)
    bot.reply_to(message, fact)
@bot.message_handler(commands=["Daily_tip"])
def send_welcome(message):
    bot.reply_to(message, """Ежегодно люди выбрасывают на свалки тонны пластика. ПЭТ-бутылки, полиэтиленовые пакеты, обрезки труб и другие отходы отравляют почву, воду и воздух. Под влиянием ультрафиолета и осадков пластик распадается на сложные химические соединения и выпадает за многие сотни километров от свалки. Чтобы решить эту проблему, нужно чтобы население сортировало отходы, люди сдавали пластик в специализированные пункты приема. Существует еще один вариант, куда деть отходы из пластика – сделать из них красивые и полезные поделки для дома и сада.""") 
@bot.message_handler(commands=['idea1'])
def send_mem(message):
    with open('ideas/idea1.png', 'rb') as f:  
        bot.send_photo(message.chat.id, f)
@bot.message_handler(commands=['idea2'])
def send_mem(message):
    with open('ideas/idea2.png', 'rb') as f:  
        bot.send_photo(message.chat.id, f)
@bot.message_handler(commands=['idea3'])
def send_mem(message):
    with open('ideas/idea3.png', 'rb') as f:  
        bot.send_photo(message.chat.id, f)
@bot.message_handler(func=lambda message: True)
def echo_message(message):
    bot.reply_to(message, message.text)
bot.infinity_polling()
